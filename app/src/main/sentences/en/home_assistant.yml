get_status:
  - (get|what is|whats|check) (the )?status (of|for) .entity_name.
  - (get|what is|whats|check) (the )?.entity_name.

get_person_location:
  - (whats|what is) .person_name. location
  - (where is|wheres) the person .person_name.

set_state_on:
  - (turn|switch) (the )?.entity_name. on

set_state_off:
  - (turn|switch) (the )?.entity_name. off

set_state_toggle:
  - (turn|switch) (the )?.entity_name. toggle
