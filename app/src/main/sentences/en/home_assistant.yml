get_status:
  - (get|what is|whats|check) (the )?(home assistant |ha )?status (of|for) .entity_name.
  - (get|what is|whats|check) .entity_name. (status|state)
  - how is .entity_name. doing
  - is .entity_name. on|off

set_state:
  - (turn|switch) .entity_name. .action.
  - (turn|switch) (the )?.entity_name. .action.
  - .action. (the )?.entity_name.
  - .entity_name. .action.

call_service:
  - (send|press) .command. (on|to) (the )?.entity_name.
  - press .command. (button )?(on|to) (the )?.entity_name.
  - send .command. to (the )?.entity_name.
  - execute .command. (on|to) (the )?.entity_name.